fn main() {
    // Placeholder until the MCP layer lands; keeps `cargo test` green while
    // the core engine is built.
    eprintln!("server entrypoint not yet implemented");
    std::process::exit(1);
}
